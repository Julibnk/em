import express from 'express';
// import {
//   create,
//   read,
//   update,
//   remove,
//   search,
//   validationSample,
// } from './category-controller';

const categoryRouter = express.Router();

// router.route('/').get('', read).post(create).put(update).delete(remove);

// categoryRouter.get('/', search);
// categoryRouter.post('/', create);

// categoryRouter.get('/:categoryId', [validationSample, read]);
// categoryRouter.patch('/:categoryId', update);
// categoryRouter.delete('/:categoryId', remove);

export default categoryRouter;
